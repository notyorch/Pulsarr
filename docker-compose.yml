version: '3.8'

services:
  telegram-alert-sender:
    build: .
    env_file:
      - .env
    ports:
      - "${PORT:-8000}:8000"
    restart: unless-stopped
    # Do not put secrets here. Populate .env (or use an external secrets manager).
